# 智能旅游助手 · 接口文档

> 版本 v1.0 ｜ 整理日期 2026-09-21 ｜ 对应代码：travel-assistant-main（Spring Boot 3.3.4）

## 一、通用说明

### 1.1 环境地址

| 环境 | 地址 | 说明 |
| --- | --- | --- |
| 开发 | `http://localhost:3200` | IDEA 直接启动后端，前端 dev server 通过 `/api` 代理到这里 |
| 生产 | `https://你的域名/api` | Nginx 网关把 `/api/xxx` 转发成后端的 `/xxx` |

写代码时前端一律用 `/api/xxx` 调用，由代理或网关去掉 `/api` 前缀，所以开发和上线不用改代码。

### 1.2 统一响应格式

所有接口（除 AI 流式对话）都返回同一个结构：

```json
{
  "success": true,
  "code": 200,
  "msg": "成功",
  "data": {},
  "error": null,
  "rawResponse": null
}
```

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| success | boolean | 是否成功 |
| code | int | 业务状态码，200 表示成功 |
| msg | string | 提示信息（失败时可直接展示给用户） |
| data | any | 业务数据，可能是对象、数组、字符串，失败时为空 |

### 1.3 状态码

| code | 含义 | 典型场景 |
| --- | --- | --- |
| 200 | 成功 | — |
| 400 | 参数错误 / 业务规则不通过 | 库存不足、重复申请、原密码错误 |
| 401 | 未登录或登录过期 | token 缺失、被登出拉黑 |
| 403 | 无权限 | 操作他人订单、非管理员访问后台 |
| 404 | 资源不存在 | 订单/行程/发票不存在 |
| 429 | 请求过于频繁 | 触发限流，响应头带 `Retry-After: 60` |
| 500 | 服务器内部错误 | 未预期异常 |

### 1.4 鉴权

登录后拿到 JWT，放在请求头里：

```
Authorization: Bearer eyJhbGciOiJIUzI1NiJ9....
```

- 用户端 token 由 `/auth/login` 返回，默认有效期 **7 天**
- 管理端 token 由 `/admin/login` 返回，带 `role=ADMIN`
- 除 `/auth/login`、`/auth/register`、`/admin/login` 外，其余接口都必须带 token
- **退出登录后 token 进 Redis 黑名单**，立刻失效（不是等自然过期）

需要登录的路径前缀：`/travel/**`、`/order/**`、`/favorite/**`、`/plan/**`、`/invoice/**`、`/notice/**`、`/admin/**`

### 1.5 接口限流

按**客户端 IP** 每分钟计数（Redis 计数，Redis 挂了自动降级为内存计数）：

| 分组 | 覆盖接口 | 阈值 |
| --- | --- | --- |
| AI 类 | `/travel/chat`、`/travel/recommend`、`/travel/intro` | 20 次/分 |
| 订单类 | `/order/**` | 30 次/分 |
| 登录类 | `/auth/login`、`/auth/register`、`/admin/login` | 10 次/分 |
| 默认 | 其余接口 | 120 次/分 |

触发限流返回 429，`data` 为空，`msg` 为「请求过于频繁，请稍后再试」。Nginx 层还有一道 `limit_req`（20r/s），形成两层防护。

### 1.6 幂等（防止重复下单）

`POST /order/create` 支持请求头 `Idempotency-Key`（建议传 UUID）：

```
Idempotency-Key: idem-1758426000000-ab12cd34
```

- 同一个用户 + 同一个键，**10 分钟内重复提交只会生成一张订单**，第二次直接返回第一次的订单
- 前端酒店/机票/火车票下单都已自动携带，用户连点、超时重发都不会重复下单

---

## 二、认证与账号（/auth）

### 2.1 注册

`POST /auth/register` ｜ 无需登录

请求体：

```json
{ "username": "zhangsan", "password": "123456", "email": "zhangsan@example.com" }
```

响应：

```json
{ "success": true, "code": 200, "msg": "成功", "data": "注册成功" }
```

说明：密码用 BCrypt 加密存储；用户名重复返回 `code=400`「注册失败，用户名可能已存在」。

### 2.2 登录

`POST /auth/login` ｜ 无需登录

```json
{ "username": "zhangsan", "password": "123456" }
```

响应（`data` 就是 token，前端存 localStorage）：

```json
{ "success": true, "code": 200, "msg": "成功", "data": "eyJhbGciOiJIUzI1NiJ9..." }
```

失败返回 `code=401`「用户名或密码错误」。

### 2.3 退出登录

`POST /auth/logout` ｜ 需要登录 ｜ 无请求体

说明：把当前 token 写入 Redis 黑名单，过期时间 = token 剩余有效期，之后该 token 一律 401。

### 2.4 修改密码

`POST /auth/change-password` ｜ 需要登录

```json
{ "oldPassword": "123456", "newPassword": "1234567" }
```

说明：新密码至少 6 位、不能与原密码相同；修改成功后当前 token 立即失效，需要重新登录。

---

## 三、AI 与内容（/travel）

### 3.1 健康检查

`GET /travel/hello` ｜ 需要登录

```json
{ "success": true, "code": 200, "msg": "成功", "data": "hello world" }
```

### 3.2 AI 行程规划

`POST /travel/recommend` ｜ 需要登录 ｜ **限流 20 次/分**

```json
{ "city": "成都", "days": 3, "budget": 3000 }
```

参数校验：`days` 1~30，`budget` ≥ 100。

响应（节选）：

```json
{
  "success": true, "code": 200, "msg": "成功",
  "data": {
    "city": "成都",
    "days": "3",
    "totalBudget": 3000.0,
    "degraded": false,
    "dailyItinerary": [
      {
        "day": 1,
        "date": "2026-10-01",
        "morning":   { "spot": "宽窄巷子", "duration": "3小时", "ticket": "免费", "transportation": "地铁4号线", "description": "..." },
        "afternoon": { "spot": "武侯祠", "duration": "2小时", "ticket": "50元", "transportation": "地铁3号线", "description": "..." },
        "evening":   { "spot": "锦里", "duration": "2小时", "ticket": "免费", "transportation": "步行", "description": "..." }
      }
    ],
    "budgetBreakdown": { "accommodation": 1200, "food": 600, "transportation": 400, "tickets": 300, "other": 500 },
    "tips": ["..."],
    "warnings": ["..."]
  }
}
```

说明：`degraded=true` 表示大模型不可用（熔断或超时），返回的是本地兜底行程，前端会提示「AI 服务暂时不可用」。

### 3.3 AI 行程助手（流式对话）

`POST /travel/chat` ｜ 需要登录 ｜ **限流 20 次/分** ｜ 响应类型 `text/event-stream`

```json
{ "message": "帮我推荐成都三日游" }
```

响应为 SSE 流，每条 `data` 是一个 JSON 片段：

```
data: {"content":"成都"}

data: {"content":"三日游"}

```

前端逐段追加实现打字机效果，连接由服务端在生成结束后关闭。熔断或超时会推送降级文本。

### 3.4 综合搜索（景点 / 酒店 / 附近）

`POST /travel/search` ｜ 需要登录

```json
{ "keyword": "上海外滩" }
```

响应 `data` 结构：

```json
{
  "keyword": "上海外滩",
  "attractions": [ { "name": "外滩", "city": "上海", "ticket": "免费", "price": "", "rating": "4.8", "address": "中山东一路", "distance": "", "tags": "", "description": "" } ],
  "hotels": [],
  "nearby": []
}
```

### 3.5 酒店列表（高德真实数据）

`GET /travel/hotels` ｜ 需要登录

| 参数 | 必填 | 说明 |
| --- | --- | --- |
| city | 否 | 城市，如「上海」 |
| keyword | 否 | 关键词，默认「酒店」，可传具体酒店名 |
| machineId | 否 | 设备号，不传 city 时用于按设备推荐城市 |

响应 `data` 为数组，元素结构：

```json
{
  "name": "上海外滩豫园美居酒店",
  "address": "黄浦区人民路",
  "distance": "1.2km",
  "tel": "021-xxxx",
  "type": "住宿服务",
  "location": "121.49,31.23",
  "rating": "4.8",
  "city": "上海",
  "cost": "680",
  "typeName": "豪华型",
  "image": "https://store.is.autonavi.com/showpic/xxxx"
}
```

### 3.6 景点列表（高德真实数据）

`GET /travel/spots` ｜ 需要登录 ｜ 参数同上（`keyword` 默认「景点」），返回结构同 3.5。

### 3.7 城市列表（旅游页「推荐城市」）

`GET /travel/cities` ｜ 需要登录

```json
{ "success": true, "code": 200, "msg": "成功",
  "data": ["北京", "上海", "广州", "成都", "..."] }
```

说明：返回 40 个常见旅游城市 + 管理端推荐库里出现过的城市（去重，默认城市在前）。

### 3.8 取一张配图

`GET /travel/image?keyword=西湖&city=杭州` ｜ 需要登录

`data` 为图片 URL 字符串（高德 POI 首图），带 Redis 缓存。

### 3.9 AI 生成简介

`GET /travel/intro?name=上海外滩&city=上海` ｜ 需要登录 ｜ **限流 20 次/分**

`data` 为一段中文简介（100~200 字），结果缓存 30 天。

### 3.10 多图相册

`GET /travel/photos?name=上海外滩豫园美居酒店&city=上海` ｜ 需要登录

```json
{ "success": true, "code": 200, "msg": "成功",
  "data": ["https://...1.jpg", "https://...2.jpg"] }
```

最多返回 6 张，缓存 7 天。酒店详情页的「1 大 4 小」相册、景点详情页相册都用它。

### 3.11 库存余量查询

`GET /travel/stock?placeId=1&date=2026-10-01` ｜ 需要登录

```json
{
  "success": true, "code": 200, "msg": "成功",
  "data": [
    { "roomType": "标准大床房", "total": 5, "sold": 2, "left": 3, "price": 428.0 },
    { "roomType": "高级大床房", "total": 3, "sold": 3, "left": 0, "price": 508.0 }
  ]
}
```

说明：酒店详情页用它显示「仅剩 N 间」，`left=0` 时预订按钮置灰。**没有返回记录的日期表示不限量**（兼容老数据）。

### 3.12 推荐库列表（管理端维护的内容）

`GET /travel/places?type=HOTEL&city=上海` ｜ 需要登录

参数：`type` 必填（`HOTEL` / `SPOT`），`city` 可选。返回 `Place` 实体数组（含 `id`、`name`、`city`、`address`、`price`、`rating`、`image`、`tags`、`description`、`sort`、`status`）。

---

## 四、订单与支付（/order）

### 4.1 创建订单

`POST /order/create` ｜ 需要登录 ｜ 请求头可带 `Idempotency-Key`

请求体（酒店示例）：

```json
{
  "type": "HOTEL",
  "fromCity": "上海外滩豫园美居酒店",
  "toCity": "上海",
  "travelDate": "2026-10-01",
  "departTime": "2026-10-02",
  "arriveTime": "",
  "seat": "标准大床房 × 1晚",
  "price": 428,
  "passenger": "zhangsan",
  "phone": "",
  "placeId": 1,
  "bizDate": "2026-10-01",
  "roomType": "标准大床房",
  "quantity": 1
}
```

| 字段 | 必填 | 说明 |
| --- | --- | --- |
| type | 是 | `HOTEL` / `SPOT` / `FLIGHT` / `TRAIN` |
| fromCity / toCity | 是 | 酒店传店名 + 城市；交通类传出发地/目的地 |
| travelDate | 是 | 出行日期 `yyyy-MM-dd`（酒店=入住日） |
| departTime | 否 | 酒店=退房日；交通类=出发时间 |
| arriveTime | 否 | 到达时间 |
| seat | 是 | 房型 / 座位舱位 / 票种描述 |
| price | 是 | 订单总价（酒店 = 每晚价 × 晚数） |
| passenger / phone | 否 | 乘客与联系电话 |
| placeId / bizDate / roomType / quantity | 否 | 库存关联字段，**传了就会占用真实库存** |

响应 `data` 为创建后的订单对象（含 `id`、`orderNo`、`payStatus=UNPAID`）。

业务规则：

- 带 `placeId/bizDate/roomType` 且该日期已配库存 → 库存不足直接返回 `code=400`「该日期「标准大床房」只剩 N 间/张，库存不足」
- 订单创建后 **30 分钟未支付会自动关闭并回补库存**（后端定时任务，每 60 秒扫一次）

### 4.2 我的订单

`GET /order/my` ｜ 需要登录 ｜ 无参数

返回当前用户订单数组，按时间倒序。订单字段说明见「附录 A 订单数据字典」。

### 4.3 订单详情

`GET /order/{id}` ｜ 需要登录

说明：只能查自己的订单，否则 `code=403`；收银台页面用它拉真实数据。

### 4.4 支付订单（沙箱）

`POST /order/pay/{id}` ｜ 需要登录

```json
{ "method": "ALIPAY" }
```

| method | 说明 |
| --- | --- |
| ALIPAY | 支付宝（默认） |
| WECHAT | 微信支付 |
| UNIONPAY | 云闪付 |
| CARD | 银行卡 |

说明：

- **幂等**：已支付订单重复调用直接返回原订单，不会重复扣款、不会重复加积分
- 订单已取消 → `code=400`「订单已取消，无法支付」
- 超过 30 分钟未支付 → 自动置为已取消并返回「订单超过 30 分钟未支付，已自动关闭」
- 支付成功会写入 `payMethod`、`payNo`（流水号）、`payTime`，并推送站内消息

### 4.5 申请改签 / 改期

`POST /order/change/{id}` ｜ 需要登录

```json
{
  "fromCity": "北京", "toCity": "上海",
  "travelDate": "2026-10-05", "departTime": "09:00", "arriveTime": "13:00",
  "seat": "二等座", "price": 553
}
```

说明：必须是**已支付、未取消、且没有待审核申请**的订单；酒店/景点只传新日期即可。提交后 `changeStatus=PENDING`，等管理员审批。

### 4.6 申请退订

`POST /order/cancel/{id}` ｜ 需要登录 ｜ 无请求体

- **未支付订单**：直接取消，立即回补库存，不需要审批
- **已支付订单**：进入待审核状态，管理员通过后订单取消并自动生成**沙箱退款**（`refundStatus=REFUNDED`、`refundNo`、`refundTime`）

---

## 五、站内消息（/notice）

下单、支付、审批结果、退款、发票开具、订单被管理员修改都会推一条消息。

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/notice/my` | GET | 我的消息（最近 50 条，按时间倒序） |
| `/notice/unread` | GET | 未读数，返回 `{ "unread": 3 }` |
| `/notice/read/{id}` | POST | 标记单条已读（只能操作自己的） |
| `/notice/read-all` | POST | 全部标为已读 |

消息对象：

```json
{ "id": 12, "type": "PAY", "title": "支付成功", "content": "订单 T1758... 已支付 ¥428.0，支付方式 WECHAT，交易流水号 PAY...", "readFlag": false, "createTime": "2026-09-21T16:20:31" }
```

`type` 取值：`ORDER`（订单）、`PAY`（支付）、`APPROVE`（审批）、`REFUND`（退款）、`SYSTEM`（系统/发票）。

---

## 六、个人中心（收藏 / 行程 / 发票）

### 6.1 收藏

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/favorite/my` | GET | 我的收藏列表 |
| `/favorite/check?type=HOTEL&name=xxx` | GET | 是否已收藏，返回 `{ "favorited": true }` |
| `/favorite/toggle` | POST | 收藏 / 取消收藏（点一次收藏，再点取消） |
| `/favorite/{id}` | DELETE | 删除收藏（只能删自己的） |

`toggle` 请求体：

```json
{ "type": "SPOT", "name": "上海外滩", "city": "上海", "address": "中山东一路", "image": "https://...", "price": 0 }
```

说明：同一用户同一地点不会重复收藏，上限 200 个。

### 6.2 行程历史

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/plan/my` | GET | 我的行程列表 |
| `/plan/{id}` | GET | 行程详情（含 `resultJson`，AI 生成的原始结果） |
| `/plan/save` | POST | 保存行程 |
| `/plan/{id}` | DELETE | 删除行程 |

`save` 请求体：

```json
{ "city": "成都", "days": 3, "budget": 3000, "resultJson": "{...AI返回的完整JSON字符串...}" }
```

说明：每个用户最多 50 条；保存成功会推送一条站内消息。

### 6.3 发票

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/invoice/my` | GET | 我的发票申请 |
| `/invoice/apply` | POST | 申请发票 |

```json
{ "orderId": 12, "title": "某某科技有限公司", "taxNo": "91370100MA3xxxxx", "email": "finance@example.com" }
```

说明：只能给**自己的、已支付的、未开过的**订单申请；`title` 必填。状态：`PENDING` 待开具 / `ISSUED` 已开具 / `REJECTED` 已驳回。

---

## 七、管理端（/admin）

> 全部接口都需要**管理员 token**（`/admin/login` 除外）。管理员账号默认 `admin / admin123`（首次启动自动创建，上线请改密码）。所有写操作都会记入**操作审计日志**。

### 7.1 管理员登录

`POST /admin/login` ｜ 无需登录

```json
{ "username": "admin", "password": "admin123" }
```

响应 `data` 为管理员 token（带 `role=ADMIN`）。

### 7.2 数据统计

`GET /admin/stats`

```json
{
  "totalUsers": 12, "totalLogins": 88, "totalRecommends": 30, "totalChats": 41,
  "todayLogins": 5, "todayRecommends": 3, "todayChats": 7,
  "yesterdayLogins": 4, "yesterdayRecommends": 2, "yesterdayChats": 6,
  "last7Days": [ { "date": "2026-09-15", "logins": 3, "recommends": 1, "chats": 2 } ],
  "recentLogs": [ { "time": "2026-09-21T16:20:31", "username": "zhangsan", "action": "LOGIN" } ]
}
```

说明：`last7Days` 是近 7 天按天聚合的登录/推荐/对话次数（按 `date` 升序，最后一项是今天）；`recentLogs` 是最近 50 条用户行为日志，管理端「数据统计」页的表格用它。

### 7.3 订单管理

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/orders?type=HOTEL` | GET | 订单列表，`type` 可选（HOTEL/SPOT/FLIGHT/TRAIN），不传返回全部 |
| `/admin/orders/{id}` | PUT | 修改订单（可改出发地、目的地、日期、时间、座位、价格、状态、乘客、电话） |
| `/admin/orders/{id}` | DELETE | 删除订单（会回补库存并记审计日志） |

`PUT` 请求体只传要改的字段即可：

```json
{ "travelDate": "2026-10-08", "departTime": "10:30", "status": "已预订" }
```

### 7.4 待处理申请（改签 / 退订审批）

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/order-changes` | GET | 待审批列表（`changeStatus=PENDING`） |
| `/admin/order-changes/{id}/approve` | POST | 通过：改签写回新信息；退订置为已取消并**自动退款** |
| `/admin/order-changes/{id}/reject` | POST | 驳回 |

说明：**重复审批会被拦截**（`code=400`「该申请已处理过，请刷新后查看」），避免并发点两次把数据改乱。

### 7.5 发票管理

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/invoices?status=PENDING` | GET | 发票申请列表，可按状态筛选 |
| `/admin/invoices/{id}/issue` | POST | 开票（通知用户） |
| `/admin/invoices/{id}/reject` | POST | 驳回（通知用户） |

### 7.6 库存管理

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/stocks?placeId=1` | GET | 某地点的全部库存配置 |
| `/admin/stocks` | POST | 新增或修改库存（同地点+日期+房型有则改，无则加） |

```json
{ "placeId": 1, "bizDate": "2026-10-01", "roomType": "标准大床房", "total": 5, "price": 428 }
```

说明：总库存不能小于已售数量，否则返回 `code=400`。

### 7.7 酒店 / 景点管理（推荐库）

| 接口 | 方法 | 说明 |
| --- | --- | --- |
| `/admin/places?type=HOTEL&city=上海&keyword=&status=1` | GET | 列表（参数都可选） |
| `/admin/places` | POST | 新增 |
| `/admin/places/{id}` | PUT | 修改 |
| `/admin/places/{id}` | DELETE | 删除 |
| `/admin/places/{id}/toggle` | POST | 上架 / 下架 |

`Place` 字段：`type`（HOTEL/SPOT）、`name`、`city`、`address`、`price`、`rating`、`image`、`tags`、`description`、`sort`（排序权重，越大越靠前）、`status`（1 上架 / 0 下架）。

### 7.8 操作审计日志

`GET /admin/logs`（最近 200 条）

```json
{ "id": 31, "adminUsername": "admin", "action": "APPROVE", "target": "T1758...", "detail": "通过退订，状态=已取消，退款单号 RF...", "createTime": "2026-09-21T16:31:02" }
```

`action` 取值：`LOGIN` 登录、`UPDATE_ORDER` 修改订单、`DELETE_ORDER` 删除订单、`APPROVE` 审批通过、`REJECT` 审批驳回、`ISSUE_INVOICE` 开票、`REJECT_INVOICE` 驳回发票、`SAVE_STOCK` 维护库存。

---

## 八、附录

### 附录 A：订单数据字典

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| id / orderNo | Long / String | 主键 / 订单号（T 开头） |
| username | String | 下单用户 |
| type | String | HOTEL / SPOT / FLIGHT / TRAIN |
| fromCity / toCity | String | 出发地 / 目的地（酒店为店名、城市） |
| travelDate / departTime / arriveTime | String | 日期与时间 |
| seat | String | 房型 / 座位舱位 / 票种 |
| price | Double | 订单总价 |
| status | String | `已预订` / `已取消` |
| payStatus | String | `UNPAID` 未支付 / `PAID` 已支付 |
| payMethod / payNo / payTime | String / String / 时间 | 支付方式 / 流水号 / 支付时间 |
| refundStatus / refundNo / refundTime | String / String / 时间 | `REFUNDED` 已退款 / 退款单号 / 退款时间 |
| changeStatus | String | 空 / `PENDING` / `APPROVED` / `REJECTED` |
| requestType | String | `CHANGE` 改签 / `CANCEL` 退订 |
| reqXxx 系列 | String | 申请修改后的新值（出发地、目的地、日期、时间、座位、价格） |
| placeId / bizDate / roomType / quantity | Long / String / String / Integer | 库存关联：地点、库存日期、房型、数量 |
| passenger / phone | String | 乘客 / 联系电话 |
| createTime / payTime / changeApplyTime | 时间 | 下单 / 支付 / 申请时间 |

### 附录 B：接口速查表

| 分组 | 接口数 | 主要接口 |
| --- | --- | --- |
| 认证账号 | 4 | register、login、logout、change-password |
| AI 与内容 | 11 | recommend、chat、search、hotels、spots、cities、image、intro、photos、stock、places |
| 订单支付 | 6 | create、my、{id}、pay、change、cancel |
| 站内消息 | 4 | my、unread、read、read-all |
| 个人中心 | 10 | favorite(4)、plan(4)、invoice(2) |
| 管理端 | 19 | login、stats、orders(3)、order-changes(3)、invoices(3)、stocks(2)、places(5)、logs(1) |

### 附录 C：现成的测试材料

项目 `deploy/测试` 目录里有：

- `接口冒烟测试.http`：18 个接口的现成用例，VS Code 装 REST Client 插件后点「Send Request」即可，也照着在 Postman/Apifox 里建集合
- `压测脚本.ps1`：并发压测，输出 QPS、平均延迟、429 数量
- `并发抢库存.ps1`：库存设 N 并发 M 个下单请求，自动断言「成功数 = 库存数」，用来证明不超卖

### 附录 D：前端调用示例

```js
// 登录
const res = await fetch('/api/auth/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'zhangsan', password: '123456' }),
}).then((r) => r.json())
localStorage.setItem('token', res.data)

// 下单（带幂等键）
await fetch('/api/order/create', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    Authorization: 'Bearer ' + localStorage.getItem('token'),
    'Idempotency-Key': 'idem-' + Date.now() + '-' + Math.random().toString(36).slice(2, 10),
  },
  body: JSON.stringify({ type: 'HOTEL', fromCity: '上海外滩豫园美居酒店', toCity: '上海',
    travelDate: '2026-10-01', departTime: '2026-10-02', seat: '标准大床房 × 1晚', price: 428 }),
})

// AI 流式对话（SSE）
const resp = await fetch('/api/travel/chat', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json', Authorization: 'Bearer ' + localStorage.getItem('token') },
  body: JSON.stringify({ message: '推荐成都三日游' }),
})
const reader = resp.body.getReader()
const decoder = new TextDecoder()
while (true) {
  const { done, value } = await reader.read()
  if (done) break
  // 解析以 "data: " 开头的行，把其中的 content 追加到消息气泡
}
```
