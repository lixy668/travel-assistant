# 告诉 Grafana 去哪里加载看板文件：这个目录下的 json 会自动变成看板
apiVersion: 1

providers:
  - name: travel
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
